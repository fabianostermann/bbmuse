# BlackBoard MUSic Engine

A blackboard system for realtime AI music generation.

This is ongoing research. The Github repository is currently set to private, but will be available soon.


## TODO list

- [ ] real multiprocessing
- [ ] Timing profiling mode (min, max, mean)

- [ ] Introduce run modes: DEBUG, DEVELOP, RELEASE \
	Debug mode: check Representations for undesired changes (using pickle), sanity check representation (introduce a function interface for that, e.g. `_sanity_check()`) \
	Develop mode: run normally. \
	Performance mode: apply optimization flags, disable gc, etc. & restart when crashed


